If the integration has issue creation permission, it can create issues from `ISSUE_PAYLOADS.json`. Otherwise, run the script locally:

```
GH_TOKEN=<your_token> python ISSUE_CREATION_SCRIPTS/create_issues.py
```
